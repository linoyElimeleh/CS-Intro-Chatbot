public enum GenreEnum {
    // ⬇︎ enum's values ⬇︎
    // Replace this line with a "ROCK" enum value
    // Replace this line with a "CLASSIC" enum value
    // Replace this line with a "BLUES" enum value
    // Replace this line with a "POP" enum value
    // Replace this line with a "RAP" enum value
    // Replace this line with a "HIP_HOP" enum value
    // ⬆︎ enum's values ⬆︎

    final String name;

    // ⬇︎ Build the enum's constructor ⬇︎
    // Replace this line the enum's constructor
    // ⬆︎ Build the enum's constructor ⬆︎

    public String getName() {
        // return the name of the genre
    }
}
